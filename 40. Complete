# include <stdio.h>
# include <stdlib.h>

int main (void) {
    
    int qtd, num, menor = 0, maior = 0;
    
    printf("Digite a quantidade de elementos para identificar o menor->");
    scanf("%i", &qtd);
    
    for(int i = 0; i < qtd; ++i){
       printf("Press [%i] number: ", i+1);
       scanf("%i", &num);
       
       
       
    }
    
    printf("O menor numero digitado foi: %i\n\n", menor);
    
    system("pause");
    return 0;
}
